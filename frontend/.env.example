# Optional. If you don't use the Vite proxy, point the client at the API directly:
# VITE_API_BASE=http://localhost:4000
VITE_API_BASE=

# Production build values live in .env.production (Vite loads it automatically
# for `npm run build`), currently VITE_API_BASE=https://gift.sunsysweb.co.in
